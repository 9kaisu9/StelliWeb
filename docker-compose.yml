services:
  backend:
    build: ./stelli-backend
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - ${HOME}/stelli:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
